{
  "name": "@metacrdt/runtime",
  "version": "0.1.0",
  "description": "MetaCRDT runtime harness: target-neutral service interfaces, operation helpers, and an in-memory target for proving multi-runtime convergence contracts.",
  "type": "module",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown src/index.ts --root src --format esm --dts --clean --platform neutral --target es2020 --unbundle",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@metacrdt/core": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
