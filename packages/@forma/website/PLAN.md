# @forma/website Plan

The canonical implementation plan lives at the repository root:

```text
../../../PLAN.md
```

Keep this package-local file as a pointer so package-scoped editors and
worktree searches still surface the plan without duplicating requirements.
